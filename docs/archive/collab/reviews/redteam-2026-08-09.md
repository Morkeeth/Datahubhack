# Red team — 2026-08-09 (merge)

Three rival runs of `docs/collab/prompts/cursor-multimodel-redteam.md`.
Weapons: **1** ninety seconds · **2** cold machine · **3** maintainer.
Deduped. `confirmed_by: N/3`. Any **3/3** outranks in-flight work.

## Kills (deduped)

| # | Kill (judge-voice) | confirmed_by | weapons | receipt (abbrev.) | survive by Mon 18:00? | owner |
|---|---|---|---|---|---|---|
| R1 | The live board is a `/tmp` JSON recording API, not GraphQL over DataHub — kill GMS and it still serves ghosts. | **3/3** | 1,2,3 | `board.py` `FileGhostStore` only; W3 stopped GMS and board still 200; W2 board counts ≠ GraphQL URNs; `AGENTS.md` hard constraint | Lane B GraphQL board (already 3/3 multimodel) — **must ship** | Lane B |
| R2 | Cold / Cloud path solidifies from `file://` after GitHub 403 — looks solid without a real PR. | **2/3** | 1,2 | W2 eval → `PEND pr_url is not a real PR`; `/tmp/nullspace-dbt-last-error.txt` 403 to `cursor[bot]`; W1 OPEN PR #1 still unmerged | Oscar: add `nullspace-dbt` to Cursor App **or** laptop `gh` as Morkeeth → new grain PR → merge → `cli finalize` | Oscar / Lane A path ready |
| R3 | Clickable dbt PR (or the thesis retell) still reads as a thin ticket→SELECT loop; OPEN PR is 17 lines of pre-think SQL. | **2/3** | 1,2 | W1 `gh pr view` OPEN +17; host `cursor` code now emits grain/`GROUP BY` but remote PR not refreshed (same 403) | Same as R2 — push grain SQL + arguing body | Oscar / Lane A |
| R4 | No upstream DataHub contribution — fork identical to master, zero PRs. | **1/3** | 3 | `gh compare … identical ahead_by:0`; handoff 005 lists OSS PR not done | Unlikely as a real mergeable OSS PR by freeze; do not fake it | Lane B / Oscar |
| R5 | Ghosts are squatted `Dataset` entities; lifecycle state is a JSON blob in `datasetProperties.customProperties` (not a PDL aspect). | **2/3** | 3×2 | Dataset squat (`ghosts.py`/`emit.py` aspects); delayed W3: `emit.py` `nullspace.demand`/`nullspace.resolution` in `customProperties` — “KV in customProperties, not an extension” | No real aspect/RFC by freeze; own the thesis or lose the maintainer room | Product law |
| R5b | Contracts (and demand board) live in `/tmp` sidecars DataHub never sees. | **1/3** | 3 | `persist.py` default `/tmp/nullspace-ghosts.json`; `contracts.py` `/tmp/nullspace-contracts.json`; `register_query` does not write contracts to GMS | Partial for single-host demo; fails “catalog is source of truth” | Lane B |
| R5c | MCP kept ghosting when GMS was down (CLI refused). | **1/3 → patched** | 3 | Was `mcp_server.py` `dh=None` → local miss; **D24:** `consumer_search` refuses unless `offline=True`, so MCP `find_dataset` no longer writes a local ghost. Stale warning string in `mcp_server.py` still claims “recorded locally” — Lane B copy fix. Board (R1) still survives GMS death. | Search path yes; board no | Lane A done / Lane B warn |
| R6 | README cold reveal builder uses disclosed hardcoded plan when no requester contracts / MCP plan exist. | **1/3** | 2 | `builder.py` fallback; idempotency `"schema_source": "disclosed fallback…"` | Maybe — judged path must register contracts before build (Lane B agents) | Lane B (+ Lane A keep disclosure) |
| R7 | `up.sh` could declare healthy with warehouse stopped → builder dies on `:5432`. | **2/3** | 2×2 | First W2 + delayed W2; **`up.sh` now waits on `pg_isready`** | Patched healthy-gate | Lane A |
| R7b | Failed build after claim strands ghost in `claimed` → retry sees 0 open ghosts. | **1/3 → patched** | 2 | Delayed W2: warehouse stop → claim → SQL fail → `DECISION: declined: 0 open ghosts` | **`release_claim` on SQL validation failure** | Lane A |
| R8 | Cold eval not rerunnable: searches `WANT.split()[0]` (`fresh-nullspace` prefix) so stack-already-up fails “exactly one ghost”; Ctrl-C/restart also corrupts receipts. | **2/3** | 2×2 | `eval_nullspace.py:158` `ghost_count(WANT.split()[0])`; delayed W2 FAIL “DataHub returns 2/3”; Ctrl-C path | Lane B owns eval file header — fix exact want/URN + `--cold` should `nullspace reset` | Lane B |
| R9 | Video / README hedges (“not a pull request”) and video script bans the two selling claims. | **1/3** | 1 | README Honest boundary; `video-script.md` must-NOT claims | Soften copy only after R2 true | Lane B submission |
| R10 | Roadmap docs inside the repo rank Nullspace poorly / say brand scores zero. | **1/3** | 1 | `docs/final-pitch.md` | `git` hygiene / don’t hand judges the strategy vault | Oscar |

### Why lone (1/3) kills were missed

- **R4 / R5b / R5c (WEAPON 3):** Only the maintainer weapon opens the fork, reads `customProperties`/`contracts.py`, and diffs MCP vs CLI. Ninety-seconds never clones; cold machine never leaves the README path for GitHub compare. Vault rule: sponsor-native findings from one model still matter — R4/R5 remain real.
- **R6–R8 (WEAPON 2 only):** Only cold machine runs the stranger scripts twice / half-up / Ctrl-C.
- **R9–R10 (WEAPON 1 only):** Only ninety-seconds stays in pitch artifacts.

Delayed second maintainer pass ([WEAPON 3 maintainer kill](bc-af180eea-899b-5fb0-a7f8-d9e646bc3167)) sharpened R5 (customProperties KV) and added R5b/R5c; merged here without re-running the other weapons.

Delayed second ninety-seconds pass ([WEAPON 1 ninety seconds kill](bc-9a2bce45-6eff-5d00-9711-0176fb86a379)) reconfirms R2/R3: the only clickable PR is still passthrough; README `file://` honesty is the second stop — no new kill beyond the merge table.

Delayed second cold-machine pass ([WEAPON 2 cold machine kill](bc-8bd9e098-34a0-510f-94d9-a80fbebd9eb2)) added R7b (claimed-forever — **patched**) and sharpened R8 (eval prefix search / not rerunnable — Lane B).

## Acted this turn (Lane A)

- **Law 2 hole closed in plumbing:** `consumer_search(..., dh=None)` now **refuses** unless `offline=True` (unit tests only). MCP’s “local ghost when GMS down” path is blocked without editing `mcp_server.py`. CLI + `./scripts/without-datahub.sh` → three isolated agents `status=refused`.
- **R7:** `scripts/up.sh` refuses if warehouse is not `pg_isready`.
- **Grain proof (host `cursor`):** deterministic `SqlPlan` for MRR → `sum("mrr") … group by segment, month`; warehouse `EXPLAIN` → `HashAggregate`. Remote PR still passthrough until R2 unblocks.

## Closing bets (side by side)

| Weapon | “If I had to bet, this submission loses because ______.” |
|---|---|
| 1 | …in ninety seconds it retells as a feature-request queue for missing tables with a dbt SELECT autocompleter — more boring than the tagline — and every artifact a rushed judge reaches for (hedged README, OPEN 17-line PR, video bans, board with no DataHub) betrays the thin loop. |
| 2 | …the cold path produces a convincing **local** artifact faster than the promised **public** proof: no real PR, a board that is not DataHub, and a builder fallback standing in for the agent loop. |
| 3 | …the “DataHub-native demand catalog” is a `/tmp` JSON file mirrored into `customProperties`, the public board never queries GMS, there is no upstream OSS contribution, and the submission script forbids filming the PR ending the judged host still cannot produce. (Second maintainer pass agrees: kill GMS → board lives; fork ahead_by 0.) |

**Convergence:** all three name the same shape — **judged surface ≠ engine** (board/PR/pitch over a local or unmerged loop). Until R1 (GraphQL board) and R2 (real merged PR → solid) are true in the room, nothing else outranks them.
