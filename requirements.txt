# Runtime deps for the hackathon build (install via scripts/install-deps.sh)
acryl-datahub>=1.0.0
datahub-agent-context>=0.1.0
mcp>=2.0.0
psycopg[binary]>=3.1.0
httpx>=0.27.0
pydantic>=2.0.0
python-dotenv>=1.0.0
rich>=13.0.0
typer>=0.12.0
fastapi>=0.110.0
uvicorn>=0.29.0
# dbt: pin <2 so pip does not pull Fusion (no Postgres adapter). See install-deps.sh.
dbt-core>=1.8,<2
dbt-postgres>=1.8,<1.10
